{
  "flivistic.fr": [
    {
      "plugin": "toddler.contentprocessors.soup",
      "config": {
        "title": [
          {
            "command": "select",
            "arguments": "title"
          }
        ]
      }
    }
  ]
}