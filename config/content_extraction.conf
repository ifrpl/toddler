{
  "http" : {
    "flivistic.fr": [
      {
        "plugin": {
          "module": "toddler.contentprocessors.soup",
          "handler": "SoupContentProcessor"
        },
        "config": {
          "title": [
            {
              "command": "select",
              "arguments": "title"
            }
          ]
        }

      }
    ]
  }
}